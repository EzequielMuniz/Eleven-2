// This file is no longer used as we are integrating directly with Calendly 
// and tracking events via window messages.

export interface LeadData {
  name: string;
  email: string;
  phone: string;
  revenue: string;
}
